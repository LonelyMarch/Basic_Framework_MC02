/**
 * @file shoot.c
 * @brief 发射机构 APP 的无业务迁移骨架。
 */

#include "shoot.h"

/**
 * @brief 初始化发射机构 APP。
 */
void ShootInit(void)
{
    /*
     * 迁移入口：注册发射命令订阅者、状态反馈发布者、摩擦轮/拨弹执行器和传感器实例。
     * 所有安全互锁的初始状态应在此明确设置为禁止输出。
     */
}

/**
 * @brief 执行一次发射机构控制更新。
 */
void ShootTask(void)
{
    /*
     * 迁移入口：读取发射命令，执行安全互锁与发射状态机，然后设置执行器目标。
     * 当前为空实现，不会产生任何发射动作。
     */
}
